rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Photos de preuve : uniquement la famille propriétaire
    match /proofs/{familyId}/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == familyId;
    }
  }
}
